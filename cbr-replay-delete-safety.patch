diff --git a/src/CBR/CbrSearchTree.h b/src/CBR/CbrSearchTree.h
--- a/src/CBR/CbrSearchTree.h
+++ b/src/CBR/CbrSearchTree.h
@@ -135,8 +135,9 @@ public:
 
         std::vector<CbrReplayFile>* replayFiles = &replayFilesIn;
         int replayIndex = 0;
-        if (tree[y][x].cbrReplayIndex == -1) {
+        const bool deletedNode = tree[y][x].cbrReplayIndex == -1;
+        if (deletedNode) {
             replayFiles = &deletedCases;
         }
         else {
@@ -153,7 +154,9 @@ public:
         float dissExtra = comparisonFunctionQuick(curGamestate, caseGamestate, caseReplay, caseData, replayIndex, caseIndex, false, curCosts, rsd.framesActive, rsd.activeReplay, rsd.activeCase, rsd.instantLearnSameReplay);
 
         if (((replayIndex != 0 || caseIndex != 0) || y == tree.size() - 1) && bestCandidate.similarity + maxRandomDiff >= dissimilarity + dissExtra) {
-            bestCaseCultivator(dissimilarity + dissExtra, replayIndex, caseIndex, caseSelector, false);
+            if (!deletedNode) {
+                bestCaseCultivator(dissimilarity + dissExtra, replayIndex, caseIndex, caseSelector, false);
+            }
             if (bestCandidate.similarity > dissimilarity + dissExtra) {
                 bestCandidate.treePos.y = y;
                 bestCandidate.treePos.x = x;
@@ -191,8 +194,9 @@ public:
 
         auto* replayFiles = &replayFilesIn;
         int replayIndex = 0;
-        if (tree[y][x].cbrReplayIndex == -1) {
+        const bool deletedNode = tree[y][x].cbrReplayIndex == -1;
+        if (deletedNode) {
             replayFiles = &deletedCases;
             replayIndex = 0;
         }
@@ -213,10 +217,12 @@ public:
             bestCandidate.treePos.y = y;
             bestCandidate.treePos.x = x;
             bestCandidate.similarity = dissimilarity + dissExtra;
-            bestCaseCultivator(bestCandidate.similarity, replayIndex, caseIndex, caseSelector, false);
+            if (!deletedNode) {
+                bestCaseCultivator(bestCandidate.similarity, replayIndex, caseIndex, caseSelector, false);
+            }
         }
         else {
-            if ((replayIndex != 0 || caseIndex != 0) && bestCandidate.similarity + maxRandomDiff > dissimilarity + dissExtra) {
+            if (!deletedNode && (replayIndex != 0 || caseIndex != 0) && bestCandidate.similarity + maxRandomDiff > dissimilarity + dissExtra) {
                 bestCaseCultivator(dissimilarity + dissExtra, replayIndex, caseIndex, caseSelector, false);
             }
         }
@@ -426,7 +432,7 @@ public:
                 else {
                     deletedCases[0].getCaseBase()->push_back(std::move(replayFiles[tree[y][x].cbrReplayIndex].getCaseBase()->at(tree[y][x].cbrIndex)));
                     tree[y][x].cbrReplayIndex = -1;
-                    tree[y][x].cbrIndex = deletedCases.size() - 1;
+                    tree[y][x].cbrIndex = static_cast<int>(deletedCases[0].getCaseBase()->size()) - 1;
                     return false;
                 }
             }
diff --git a/src/CBR/CbrData.cpp b/src/CBR/CbrData.cpp
--- a/src/CBR/CbrData.cpp
+++ b/src/CBR/CbrData.cpp
@@ -116,8 +116,12 @@ int CbrData::CBRcomputeNextAction(Metadata* curGamestate) {
 
 
     CbrReplayFile* replay = &instantLearnReplay;
-    if (activeReplay != -1 && !activeInstantLearn && replayFiles.size() >= 1) {
-         replay = &replayFiles[activeReplay];
+    if (activeReplay != -1 && !activeInstantLearn) {
+        if (activeReplay < 0 || activeReplay >= static_cast<int>(replayFiles.size())) {
+            resetCbr();
+            return 5;
+        }
+        replay = &replayFiles[activeReplay];
     }
 
     //If the AI misses an attack that was meant to hit we wait untill the attack is done whiffing or untill it hits the opponent to search for the next case
@@ -190,7 +194,15 @@ int CbrData::CBRcomputeNextAction(Metadata* curGamestate) {
         bool nextInstantLearnSameReplay = true;
         if (bestReplay != -1) {
             if (!selectedCase.instantLearn) {
+                if (selectedCase.replayIndex < 0 || selectedCase.replayIndex >= static_cast<int>(replayFiles.size())) {
+                    resetCbr();
+                    return 5;
+                }
                 bestReplayFile = &replayFiles[selectedCase.replayIndex];
+            }
+            if (selectedCase.caseIndex < 0 || selectedCase.caseIndex >= bestReplayFile->getCaseBaseLength()) {
+                resetCbr();
+                return 5;
             }
             bestFrame = bestReplayFile->getCase(selectedCase.caseIndex)->getStartingIndex();
         }
